# Copy this file to 'secrets' and fill in your values
# DO NOT commit the 'secrets' file to git!

# Generate password hash:
# mkpasswd -m sha-512 'YOUR_PASSWORD'
PASSWORD_HASH=

# Claude Code credentials (copy from ~/.claude/.credentials.json)
# Get it with: cat ~/.claude/.credentials.json
CLAUDE_CREDENTIALS=
